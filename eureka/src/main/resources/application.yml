server:
  port: 7001

eureka:
  instance:
    hostname: eureka7001.com #eureka服务端实例名

  client:
    register-with-eureka: false #是否向eureka注册中心注册自己，服务端不需要
    fetch-registry: false #如果为false表示自己是注册中心
    service-url: #监控页面
      defaultZone: http://localhost:7001/eureka/
